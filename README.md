vero-blog
=========

##install

`npm install`

`gem install jekyll`

`gulp`


## Deploy to gh-pages
*Commit master first*

`git subtree push --prefix dist origin gh-pages`

## Config

in _config.yml

url

title

tagline (seo description)

description (sidebar description)